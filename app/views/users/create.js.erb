<% if @user.errors.none? %>
	$('#new_item_line').before('<%= escape_javascript(render(@user)) %>')
	$('#new_user')[0].reset()
	setup()
	flash_green('<%= escape_javascript flash[:notice] %>')
<% else %>
	flash_red('<%= escape_javascript flash[:notice] %>')
<% end %>